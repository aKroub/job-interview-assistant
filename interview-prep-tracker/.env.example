# Copy this file to .env.local and fill in your values.
# .env.local is gitignored and will never be committed.
#
# CRA replaces %REACT_APP_*% tokens in public/index.html at build time.
REACT_APP_TITLE=Interview Prep Tracker
